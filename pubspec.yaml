name: dd_blog_flutter
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.17.3 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.5
  riverpod: ^1.0.3
  hooks_riverpod: ^1.0.4
  dd_js_util:
    path: ../../../ldd/dd_js_util
  flutter_smart_dialog: ^4.5.3+6
  dd_check_plugin: ^0.0.6
  equatable: ^2.0.3
  flutter_svg: ^1.1.1+1
  markdown_widget: ^1.2.9

  logger: ^1.1.0

  flutter_easyrefresh:
    git:
      url: https://github.com/hkloudou/flutter_easyrefresh.git
      ref: v2-flutter3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true

  assets:
     - assets/images/
     - assets/svgs/
